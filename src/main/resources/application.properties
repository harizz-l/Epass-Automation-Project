spring.application.name=epass-management

# SHOW DATABASES LIKE 'epass_db';

# MySQL connection
spring.datasource.url=jdbc:mysql://localhost:3306/epass_db?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=12345678

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Server port
server.port=8080




#CREATE DATABASE epass_db;
#USE epass_db;

#CREATE TABLE epass (
# id BIGINT AUTO_INCREMENT PRIMARY KEY,
#  name VARCHAR(255),
#  vehicle_number VARCHAR(255),
 # mobile_number VARCHAR(255),
  #aadhar_number VARCHAR(255),
  #place_to_visit VARCHAR(255),
  #date_in DATE,
  #date_out DATE,
# created_at DATETIME(6)
#);
